import React, { useState } from 'react';
import { Search, Filter, SortAsc, X } from 'lucide-react';
import { ListingCard } from '../ui/ListingCard';
import { Button } from '../ui/Button';
import { FilterSection, FilterInput, FilterCheckbox } from './components/FilterPane';
import { cn } from '../../lib/utils';

export const DiscoveryPage = () => {
  const [isFilterOpen, setIsFilterOpen] = useState(false);
  const [searchQuery, setSearchQuery] = useState('');

  return (
    <div className="min-h-screen bg-slate-50">
      {/* SEARCH HEADER */}
      <header className="sticky top-0 z-30 bg-white border-b border-slate-200 px-4 py-4">
        <div className="max-w-7xl mx-auto flex items-center gap-4">
          <div className="flex-1 relative">
            <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={20} />
            <input
              type="text"
              placeholder="Search properties, land, cars..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="w-full pl-10 pr-4 py-2.5 bg-slate-100 border-none rounded-xl outline-none focus:ring-2 focus:ring-emerald-500/20 transition-all text-slate-900"
            />
          </div>
          <Button
            variant="secondary"
            className="hidden md:flex items-center gap-2"
            onClick={() => setIsFilterOpen(true)}
          >
            <Filter size={18} />
            Filters
          </Button>
          <Button
            variant="secondary"
            className="md:hidden"
            onClick={() => setIsFilterOpen(true)}
          >
            <Filter size={18} />
          </Button>
        </div}
      </header>

      <main className="max-w-7xl mx-auto px-4 py-8 flex gap-8">
        {/* DESKTOP FILTER PANE */}
        <aside className={cn(
          "hidden lg:block w-80 shrink-0 space-y-8",
          isFilterOpen ? "block" : "block"
        )}>
          <div className="bg-white p-6 rounded-2xl border border-slate-200 sticky top-24">
            <div className="flex justify-between items-center mb-6">
              <h2 className="text-lg font-bold text-slate-900">Filters</h2>
              <Button variant="ghost" className="text-xs text-emerald-600" onClick={() => {}}>
                Reset All
              </Button>
            </div>

            {/* Universal Filters */}
            <FilterSection title="General">
              <FilterInput label="Location" placeholder="e.g. Kicukiro" />
              <div className="grid grid-cols-2 gap-2">
                <FilterInput label="Min Price" />
                <FilterInput label="Max Price" />
              </div>
            </FilterSection>

            {/* Category-Aware Filters (Example: Property) */}
            <FilterSection title="Property Details">
              <div className="grid grid-cols-2 gap-3">
                <FilterInput label="Beds" />
                <FilterInput label="Baths" />
              </div>
              <FilterCheckbox label="Furnished" checked={false} onChange={() => {}} />
              <FilterCheckbox label="Parking" checked={true} onChange={() => {}} />
              <FilterCheckbox label="Garden" checked={false} onChange={() => {}} />
            </FilterSection>

            <FilterSection title="Trust Level">
              <FilterCheckbox label="Verified Only" checked={true} onChange={() => {}} />
              <FilterCheckbox label="Professional Inspection" checked={false} onChange={() => {}} />
            </FilterSection>

            <Button className="w-full mt-6 py-3 rounded-xl font-semibold">
              Apply Filters
            </Button>
          </div>
        </aside>

        {/* RESULTS AREA */}
        <section className="flex-1">
          <div className="flex justify-between items-center mb-6">
            <div>
              <h1 className="text-2xl font-bold text-slate-900">Discoveries</h1>
              <p className="text-slate-500 text-sm">Showing 1,248 results for your search</p>
            </div>
            <div className="flex items-center gap-2 bg-white border border-slate-200 p-1 rounded-lg">
              <Button variant="ghost" className="text-xs py-1 px-3">Newest</Button>
              <Button variant="ghost" className="text-xs py-1 px-3">Price: Low-High</Button>
            </div>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
            {[1, 2, 3, 4, 5, 6].map((i) => (
              <ListingCard
                key={i}
                listing={{
                  id: `listing-${i}`,
                  title: i === 1 ? 'Modern 3 Bedroom House' : 'Prime Land Plot',
                  price: 85000000,
                  currency: 'RWF',
                  location: 'Kicukiro, Kigali',
                  listing_type: i === 1 ? 'sale' : 'land',
                  verification_level: i % 2 === 0 ? 'verified' : 'none',
                  media: [{ url: 'https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&q=80&w=400', category: 'Exterior' }],
                  specs: { beds: 3, baths: 2, size: '240m²' },
                }}
              />
            ))}
          </div}
        </section>
      </main>

      {/* MOBILE FILTER DRAWER */}
      {isFilterOpen && (
        <div className="fixed inset-0 z-50 lg:hidden">
          <div className="absolute inset-0 bg-slate-900/40 backdrop-blur-sm" onClick={() => setIsFilterOpen(false)} />
          <div className="absolute right-0 top-0 h-full w-full max-w-sm bg-white shadow-2xl p-6 overflow-y-auto animate-in slide-in-from-right duration-300">
            <div className="flex justify-between items-center mb-8">
              <h2 className="text-xl font-bold text-slate-900">Filters</h2>
              <Button variant="ghost" onClick={() => setIsFilterOpen(false)}>
                <X size={24} />
              </Button>
            </div>

            {/* Reuse Desktop Filter Content */}
            <FilterSection title="General">
              <FilterInput label="Location" />
              <div className="grid grid-cols-2 gap-2">
                <FilterInput label="Min Price" />
                <FilterInput label="Max Price" />
              </div>
            </FilterSection>

            <FilterSection title="Property Details">
              <div className="grid grid-cols-2 gap-3">
                <FilterInput label="Beds" />
                <FilterInput label="Baths" />
              </div>
              <FilterCheckbox label="Furnished" checked={false} onChange={() => {}} />
            </FilterSection>

            <Button className="w-full mt-8 py-4 rounded-xl font-semibold text-lg">
              Apply Filters
            </Button>
          </div>
        </div}
      )}
    </div>
  );
};
